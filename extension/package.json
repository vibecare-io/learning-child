{
  "name": "extension",
  "private": true,
  "type": "module",
  "scripts": { "build": "bun build.mjs", "build:release": "bun build.mjs --release", "e2e": "playwright test" },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/chrome": "^0.0.280",
    "@playwright/test": "^1.48.0"
  }
}
