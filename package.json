{
  "name": "learning-child",
  "private": true,
  "engines": { "node": ">=20" },
  "workspaces": ["catalog-pipeline", "extension"],
  "scripts": {
    "test": "vitest run",
    "build": "bun run --cwd catalog-pipeline build && bun run --cwd extension build",
    "typecheck": "tsc -p catalog-pipeline --noEmit && tsc -p extension --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0",
    "@types/node": "^20.0.0"
  }
}
